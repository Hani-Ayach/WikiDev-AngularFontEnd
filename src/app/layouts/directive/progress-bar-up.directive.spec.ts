import { ProgressBarUpDirective } from './progress-bar-up.directive';

describe('ProgressBarUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressBarUpDirective();
    expect(directive).toBeTruthy();
  });
});
